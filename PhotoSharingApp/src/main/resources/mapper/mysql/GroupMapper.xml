<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="portfolio.PhotoSharingApp.repository.GroupMapper">

	<!-- <resultMap type="portfolio.PhotoSharingApp.entity.Groups" id="GroupsTable">
		<id column="id" property="id" />
		<result column="group_name" property="group_name" />
	</resultMap> -->
	
	<!-- アカウント登録 -->
	<insert id="insertGroupName">
		insert into groups(
			 group_name
			 ,account_id
		)VALUES(
			 #{groups.group_name}
			 ,#{groups.account_id}
		)
	</insert>
	
	<!-- insert into groups(group_name)VALUES(テストinsert); -->

</mapper>