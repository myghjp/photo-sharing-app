<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="portfolio.PhotoSharingApp.repository.PhotoMapper">

	<resultMap type="portfolio.PhotoSharingApp.entity.Photos" id="PhotosTable">
		<id column="id" property="id"/>
		<result column="album_id" property="albumId"/>
		<result column="photo" property="photo"/>
		<result column="account_id" property="accountId"/>
		<result column="create_date_time" property="createDateTime"/>
		<association property="accounts" resultMap="accountsTable" columnPrefix="accounts_"/>
	</resultMap>
	
	<resultMap type="portfolio.PhotoSharingApp.entity.Accounts" id="accountsTable">
		<id column="id" property="id" />
		<result column="user" property="username"/>
	</resultMap>
	
	<!-- ーーーListPhotoーーー -->
	
	<!-- photosテーブル情報とアカウント名を取得 -->
	<select id="selectPhotoItems" resultMap="PhotosTable">
		select 
		photos.id
		,accounts.user as accounts_user
		,photo
		,photos.create_date_time
		from photos
			left join accounts
				on photos.account_id = accounts.id
			left join albums
				on album_id = photos.id
		    where album_id = #{id}
	</select>
	
	<insert id="insert">
		insert into photos(
			 album_id
			,photo
			,account_id
		)VALUES(
			 #{photos.albumId}
			,#{photos.photo}
			,#{photos.accountId}
		)
	</insert>
	
	<!-- ーーーDeletePhotoーーー -->
	
	<!-- このidと画像パス情報を取得 -->
	<select id="select" resultMap="PhotosTable">
		select id,photo
		from photos
		where id = #{id}
	</select>
	
	<delete id="delete">
		delete from photos
		where id = #{id}
	</delete>
	
	
	<!-- バリデーション作成中-->
	<select id="selectByAlbumId" resultType="Integer">
		select album_id
		from photos
		where id = #{id}
	</select>
	
	<select id="selectByAccountId" resultType="Integer">
		select account_id
		from photos
		where id = #{id}
	</select>
	
	
</mapper>